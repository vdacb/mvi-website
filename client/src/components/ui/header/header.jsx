import React from "react";
import MenuIcon from "./../menuIcon/menuIcon";
import "./header.css";

const Header = (props) => (
    <header>
        <MenuIcon />
        <div>EN ----- PT</div>
    </header>
)

export default Header;